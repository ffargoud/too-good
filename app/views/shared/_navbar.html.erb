<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to "#", class: "navbar-brand" do %>
    <%= image_tag "https://static.thenounproject.com/png/1911779-200.png" %>
    <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div id="navbar-title">
    <h3 class="navbar-name">Zero Food Waste</h3>
  </div>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Home", "/#", class: "nav-link" %>
        </li>

        <li class="nav-item dropdown">
          <% if policy(Restaurant).create? %>
            <p class="dropdown-item"> <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
  New restaurant
</button></p>
          <% end %>
         </li>

        <li class="nav-item dropdown">
          <%= image_tag "https://www.cstitches.com/wp-content/uploads/2019/05/no_avatar.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>

      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "btn btn-ghost" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
            <%= render partial: 'restaurants/modal', new_restaurant: @new_restaurant %>
